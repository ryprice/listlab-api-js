export default class ListShare {

    public listId: number;

    public userId: number;

    public constructor(listId: number, userId: number) {
        this.listId = listId;
        this.userId = userId;
    }
}
